<UserControl x:Class="WizMes_WellMade.Win_ord_InOutSum_Q"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WizMes_WellMade"
             mc:Ignorable="d"
             d:DesignHeight="620" d:DesignWidth="820" Loaded="Window_InOutTotalGrid_Loaded">

    <!--#region  그리드 리스트로 그리기-->
    <UserControl.Resources>
        <!-- 셀 병합 그리드 <InOutTotalGrid_Period>-->
        <Color x:Key="customBlue"  A="255"   R="54" G="95" B="177"  />
        <SolidColorBrush x:Key="customBlueBrush" Color="{StaticResource customBlue}"></SolidColorBrush>
        <SolidColorBrush x:Key="customBlueBrushOpacity" Color="LightGray" Opacity="0.11"></SolidColorBrush>

        <Style x:Key="noStyleToListboxItem" TargetType="ListBoxItem">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border>
                            <ContentPresenter></ContentPresenter>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <!--#endregion-->

    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="88*"/>
            <RowDefinition Height="500*"/>
            <RowDefinition Height="32*"/>
        </Grid.RowDefinitions>

        <!-- 화면 상단 / 조회(검색) 조건 화면라인 -->
        <Grid Margin="1" Grid.Column="0" Grid.Row="0" Background="{DynamicResource WizMes_WellMade}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100*" />
                <ColumnDefinition Width="160*" />
                <ColumnDefinition Width="180*" />
                <ColumnDefinition Width="180*" />
                <ColumnDefinition Width="200*" />
            </Grid.ColumnDefinitions>

            <!-- 기준 일자, 날짜버튼 모음-->
            <Grid Margin="1" Grid.Column="1" Grid.Row="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="75*" />
                        <ColumnDefinition Width="85*" />
                    </Grid.ColumnDefinitions>

                    <Label  Grid.Column="0" Grid.Row="0"  Style="{DynamicResource LabelSearch}">
                        <StackPanel Style="{DynamicResource StackPanelInLabel}" >
                            <Viewbox Style="{DynamicResource ViewBoxSetMaxMinHeight}">
                                <CheckBox x:Name="chkDateSrh" VerticalAlignment="Center" IsChecked="True" IsEnabled="False"/>
                            </Viewbox>
                            <Viewbox Style="{DynamicResource ViewBoxSetMaxMinHeight}" >
                                <TextBlock Text="기준일자" Style="{DynamicResource TextBlockInViewBoxSearch}" />
                            </Viewbox>
                        </StackPanel>
                    </Label>
                    <DatePicker x:Name="dtpFromDate" Grid.Column="2" Grid.Row="0" Style="{DynamicResource DatePickerSearch}" IsEnabled="True"/>
                    <DatePicker x:Name="dtpToDate"  Grid.Column="2" Grid.Row="1" Style="{DynamicResource DatePickerSearch}" IsEnabled="True"/>
                </Grid>

                <Grid Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button x:Name="btnLastMonth"  Grid.Column="2" Grid.Row="0" Click="btnLastMonth_Click">
                        <TextBlock Text="전월"  />
                    </Button>
                    <Button x:Name="btnThisMonth"  Grid.Column="3" Grid.Row="0" Click="btnThisMonth_Click">
                        <TextBlock Text="금월"  />
                    </Button>
                    <Button x:Name="btnYesterday"  Grid.Column="0" Grid.Row="0" Click="btnYesterday_Click">
                        <TextBlock Text="전일"  />
                    </Button>
                    <Button x:Name="btnToday"  Grid.Column="1" Grid.Row="0" Click="btnToday_Click">
                        <TextBlock Text="금일"  />
                    </Button>
                </Grid>
            </Grid>

            <!-- 제품그룹, 품명 모음 -->
            <Grid Margin="1" Grid.Column="2" Grid.Row="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="75*" />
                    <ColumnDefinition Width="90*" />
                    <ColumnDefinition Width="20*" />
                </Grid.ColumnDefinitions>

                <!--거래처-->
                <Grid Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="75*" />
                        <ColumnDefinition Width="90*" />
                        <ColumnDefinition Width="20*" />
                    </Grid.ColumnDefinitions>
                    <Label  Grid.Column="0" Grid.Row="0" MouseLeftButtonUp="CommonControl_Click" Style="{DynamicResource LabelSearch}">
                        <StackPanel Style="{DynamicResource StackPanelInLabel}">
                            <Viewbox Style="{DynamicResource ViewBoxSetMaxMinHeight}">
                                <CheckBox x:Name="chkCustomIDSrh"  Click="CommonControl_Click" ></CheckBox>
                            </Viewbox>
                            <Viewbox Style="{DynamicResource ViewBoxSetMaxMinHeight}">
                                <TextBlock Text="거 래 처" Style="{DynamicResource TextBlockInViewBoxSearch}"/>
                            </Viewbox>

                        </StackPanel>
                    </Label>
                    <TextBox x:Name="txtCustomIDSrh" Grid.Column="1" Grid.Row="0" Style="{DynamicResource TextBoxSearch}" 
                    KeyDown="txtCustomIDSrh_KeyDown" />
                    <Button x:Name="btnCustomIDSrh"  Grid.Column="3" Grid.Row="0" Click="btnCustomIDSrh_Click" IsEnabled="False">
                        <Image Source="{DynamicResource btiPlusFind}" Style="{DynamicResource imgPlusFind}"/>
                    </Button>
                </Grid>

                <!--품명-->
                <Grid Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="75*" />
                        <ColumnDefinition Width="90*" />
                        <ColumnDefinition Width="20*" />
                    </Grid.ColumnDefinitions>
                    <Label  Grid.Column="0" Grid.Row="1" MouseLeftButtonUp="CommonControl_Click" Style="{DynamicResource LabelSearch}">
                        <StackPanel Style="{DynamicResource StackPanelInLabel}">
                            <Viewbox Style="{DynamicResource ViewBoxSetMaxMinHeight}">
                                <CheckBox x:Name="chkArticleIDSrh"  Click="CommonControl_Click"></CheckBox>
                            </Viewbox>
                            <Viewbox Style="{DynamicResource ViewBoxSetMaxMinHeight}">
                                <TextBlock Text="품     명" Style="{DynamicResource TextBlockInViewBoxSearch}"/>
                            </Viewbox>

                        </StackPanel>
                    </Label>
                    <TextBox x:Name="txtArticleIDSrh"  Grid.Column="1" Grid.Row="1" Style="{DynamicResource TextBoxSearch}"
                         KeyDown="txtArticleIDSrh_KeyDown"/>
                    <Button x:Name="btnArticleIDSrh"  Grid.Column="3" Grid.Row="1" Click="btnArticleIDSrh_click" IsEnabled="False">
                        <Image Source="{DynamicResource btiPlusFind}" Style="{DynamicResource imgPlusFind}"/>
                    </Button>
                </Grid>


                <!--품번-->
                <Grid Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="75*" />
                        <ColumnDefinition Width="90*" />
                        <ColumnDefinition Width="20*" />
                    </Grid.ColumnDefinitions>
                    <Label  Grid.Column="0" Grid.Row="2" MouseLeftButtonUp="CommonControl_Click" Style="{DynamicResource LabelSearch}">
                        <StackPanel Style="{DynamicResource StackPanelInLabel}">
                            <Viewbox Style="{DynamicResource ViewBoxSetMaxMinHeight}">
                                <CheckBox x:Name="chkBuyerArticleNoSrh"  Click="CommonControl_Click"></CheckBox>
                            </Viewbox>
                            <Viewbox Style="{DynamicResource ViewBoxSetMaxMinHeight}">
                                <TextBlock Text="품     번" Style="{DynamicResource TextBlockInViewBoxSearch}"/>
                            </Viewbox>

                        </StackPanel>
                    </Label>
                    <TextBox x:Name="txtBuyerArticleNoSrh"  Grid.Column="1" Grid.Row="2" Style="{DynamicResource TextBoxSearch}"
                         KeyDown="txtBuyerArticleNoSrh_KeyDown"/>
                    <Button x:Name="btnBuyerArticleNoSrh"  Grid.Column="3" Grid.Row="2" Click="btnBuyerArticleNoSrh_Click" IsEnabled="False">
                        <Image Source="{DynamicResource btiPlusFind}" Style="{DynamicResource imgPlusFind}"/>
                    </Button>
                </Grid>







            </Grid>

            <!-- 관리번호, 출고구분 모음 -->
            <Grid Margin="1" Grid.Column="3" Grid.Row="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="75*" />
                    <ColumnDefinition Width="90*" />
                    <ColumnDefinition Width="20*" />
                </Grid.ColumnDefinitions>

                <!--관리번호, 오더번호-->
                <Grid Grid.Row="0" Grid.ColumnSpan="3">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="95*"/>
                        <ColumnDefinition Width="95*"/>
                        <ColumnDefinition Width="20*"/>
                    </Grid.ColumnDefinitions>
                    <Label  Grid.Column="0" Grid.Row="0" MouseLeftButtonUp="CommonControl_Click" Style="{DynamicResource LabelSearch}">
                        <StackPanel Style="{DynamicResource StackPanelInLabel}">
                            <Viewbox Style="{DynamicResource ViewBoxSetMaxMinHeight}">
                                <CheckBox x:Name="chkOrderIDSrh" Click="CommonControl_Click"/>
                            </Viewbox>
                            <Viewbox Style="{DynamicResource ViewBoxSetMaxMinHeight}">
                                <TextBlock x:Name="tblOrderIDSrh" Text=" 관리번호" Style="{DynamicResource TextBlockInViewBoxSearch}"/>
                            </Viewbox>
                        </StackPanel>
                    </Label>
                    <TextBox x:Name="txtOrderIDSrh"  Grid.Column="1" Grid.Row="0" Padding="0" Style="{DynamicResource TextBoxSearch}"/>
                </Grid>


                <!--라디오버튼-->
                <Border Grid.ColumnSpan="2" Grid.Column="0" Grid.Row="1" Margin="1"
                        HorizontalAlignment="Stretch" Background="#ededed" BorderBrush="Gray" BorderThickness="1">
                    <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>

                        <RadioButton x:Name="rbnOrderNOSrh" Grid.Column="0" Margin="-5,0,0,0"
                                 Click="rbnOrderNOSrh_Click" HorizontalAlignment="Center" VerticalAlignment="Center" >
                            <TextBlock Text="Order No." Style="{DynamicResource TextBlockInViewBoxSearch}" Margin="0"/>
                        </RadioButton>
                        <RadioButton x:Name="rbnOrderIDSrh" Grid.Column="1" Margin="-5,0,0,0" IsChecked="True"
                                 Click="rbnOrderIDSrh_Click" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="관리번호" Style="{DynamicResource TextBlockInViewBoxSearch}" Margin="0"/>
                        </RadioButton>
                    </Grid>
                </Border>

                <!--입출고구분-->
                <Grid Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Visibility="Hidden">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="75*" />
                        <ColumnDefinition Width="90*" />
                        <ColumnDefinition Width="20*" />
                    </Grid.ColumnDefinitions>
                    <Label  Grid.Column="0" Grid.Row="0" MouseLeftButtonUp="CommonControl_Click" Style="{DynamicResource LabelSearch}">
                        <StackPanel Style="{DynamicResource StackPanelInLabel}">
                            <Viewbox Style="{DynamicResource ViewBoxSetMaxMinHeight}">
                                <CheckBox x:Name="chkInOutGubunSrh" Click="CommonControl_Click" ></CheckBox>
                            </Viewbox>
                            <Viewbox Style="{DynamicResource ViewBoxSetMaxMinHeight}">
                                <TextBlock Text="입출고구분" Style="{DynamicResource TextBlockInViewBoxSearch}" />
                            </Viewbox>

                        </StackPanel>
                    </Label>
                    <ComboBox x:Name="cboInOutGubunSrh" Grid.Column="1" Grid.Row="0" Style="{DynamicResource ComboBoxSearchString}"/>
                </Grid>



                <!--입고검수구분-->
                <Grid Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" Visibility="Hidden">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="75*" />
                        <ColumnDefinition Width="90*" />
                        <ColumnDefinition Width="20*" />
                    </Grid.ColumnDefinitions>
                    <Label  Grid.Column="0" Grid.Row="0" MouseLeftButtonUp="CommonControl_Click" Style="{DynamicResource LabelSearch}">
                        <StackPanel Style="{DynamicResource StackPanelInLabel}">
                            <Viewbox Style="{DynamicResource ViewBoxSetMaxMinHeight}">
                                <CheckBox x:Name="chkInInsepectGubunSrh" Click="CommonControl_Click" ></CheckBox>
                            </Viewbox>
                            <Viewbox Style="{DynamicResource ViewBoxSetMaxMinHeight}">
                                <TextBlock Text="입고검수구분" Style="{DynamicResource TextBlockInViewBoxSearch}" />
                            </Viewbox>

                        </StackPanel>
                    </Label>
                    <ComboBox x:Name="cboInInspectGubunSrh"  Grid.Column="1" Grid.Row="0" Style="{DynamicResource ComboBoxSearchString}" />
                </Grid>


                <!--주요관심품목-->
                <Grid Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Visibility="Hidden">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="75*" />
                        <ColumnDefinition Width="90*" />
                        <ColumnDefinition Width="20*" />
                    </Grid.ColumnDefinitions>
                    <Label Grid.Column="0" Grid.Row="0" Style="{DynamicResource LabelSearch}">
                        <StackPanel Style="{DynamicResource StackPanelInLabel}">
                            <Viewbox Style="{DynamicResource ViewBoxSetMaxMinHeight}">
                                <CheckBox x:Name="chkMainInterestItemSrh" Click="CommonControl_Click" ></CheckBox>
                            </Viewbox>
                            <Viewbox Style="{DynamicResource ViewBoxSetMaxMinHeight}">
                                <TextBlock Text="주요 관심 품목" Style="{DynamicResource TextBlockInViewBoxSearch}" />
                            </Viewbox>
                        </StackPanel>
                    </Label>
                </Grid>


                <!--거래처별 등록 품목-->
                <Label Grid.Column="0" Grid.Row="1"  Style="{DynamicResource LabelSearch}" MouseLeftButtonDown="CommonControl_Click"
                       Visibility="Hidden">
                    <StackPanel Style="{DynamicResource StackPanelInLabel}">
                        <Viewbox Style="{DynamicResource ViewBoxSetMaxMinHeight}">
                            <CheckBox x:Name="chkCustomsEnrollItem" Click="CommonControl_Click" ></CheckBox>
                        </Viewbox>
                        <Viewbox Style="{DynamicResource ViewBoxSetMaxMinHeight}">
                            <TextBlock Text="거래처별 등록 품목" Style="{DynamicResource TextBlockInViewBoxSearch}" />
                        </Viewbox>

                    </StackPanel>
                </Label>
            </Grid>

            <!-- 검색/닫기/엑셀 버튼 -->
            <Grid Grid.Column="4" Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- 검색(조회) 버튼 -->
                <Button x:Name="btnSearch" Grid.Column="2" Grid.Row="0" 
                        Click="btnSearch_Click" Margin="1">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{DynamicResource btiSearch}" Style="{DynamicResource imgSize}" VerticalAlignment="Center" />
                        <TextBlock Text="{Binding Source={StaticResource tblSearch},Mode=OneWay,Path=Text}" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>

                <!-- 닫기 버튼 -->
                <Button x:Name="btnClose"  Grid.Column="3" Grid.Row="0" Click="btnClose_Click" Margin="1" >
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{DynamicResource btiClose}" Style="{DynamicResource imgSize}" VerticalAlignment="Center" />
                        <TextBlock Text="{Binding Source={StaticResource tblClose},Mode=OneWay,Path=Text}" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>

                <!-- 엑셀 버튼 -->
                <Button x:Name="btnExcel"  Grid.Column="3" Grid.Row="1" Click="btnExcel_Click" Margin="1">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{DynamicResource btiExcel}" Style="{DynamicResource imgSize}" VerticalAlignment="Center" />
                        <TextBlock Text="{Binding Source={StaticResource tblExcel},Mode=OneWay,Path=Text}" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
            </Grid>
        </Grid>

        <!-- 화면 중단, 데이터그리드 -->
        <Grid Margin="2" Grid.Column="0" Grid.Row="1">
            <UniformGrid Columns="4" Margin="5,0">
                <FrameworkElement x:Name="c1"/>
            </UniformGrid>

            <TabControl x:Name="tabconGrid" Grid.ColumnSpan="4" SelectionChanged="tabconGrid_SelectionChanged">
                <TabItem x:Name="tiPeriod"  Header="기간집계" Width="{Binding Path=ActualWidth, ElementName=c1}" Height="25"  >
                    <Grid>
                        <Grid.RowDefinitions>
                            <!--<RowDefinition Height="auto" />-->
                            <RowDefinition  />
                            <RowDefinition Height="0.13*"/>
                        </Grid.RowDefinitions>
                        <DataGrid x:Name="grdPeriod" IsReadOnly="True" Grid.ColumnSpan="2"  Style="{DynamicResource DataGridSet1}"                                 
                                  SizeChanged="DataGrid_SizeChange" >
                            <DataGrid.Resources>
                                <Style TargetType="DataGridRow">
                                    <Style.Resources>
                                        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#3399FF"/>
                                        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="White"/>
                                        <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#3399FF" />
                                        <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="White"/>
                                    </Style.Resources>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding P_Color1}" Value="true">
                                            <Setter Property="Background" Value="#D8D8D8"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding P_Color2}" Value="true">
                                            <Setter Property="Background" Value="#BDBDBD"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.Resources>

                            <DataGrid.Columns>
                                <DataGridTextColumn Header="순" Binding="{Binding P_NUM}" />
                                <DataGridTextColumn Header="구분" Binding="{Binding P_Gbn}" ElementStyle="{StaticResource DataGridCellCenter}" />
                                <DataGridTextColumn Header="거래처" Binding="{Binding P_CustomName}" Width="*" ElementStyle="{StaticResource DataGridCellCenter}" />
                                <DataGridTextColumn Header="품명" Binding="{Binding P_Article}" Width="1.2*"/>
                                <DataGridTextColumn Header="건수" Binding="{Binding P_Roll}" Width="*"
                                  ElementStyle="{StaticResource DataGridCellRight}"/>
                                <DataGridTextColumn Header="수량" Binding="{Binding P_Qty}" Width="*"
                                  ElementStyle="{StaticResource DataGridCellRight}"/>
                                <DataGridTextColumn Header="단위" Binding="{Binding P_UnitClssName}" Width="*"
                                  ElementStyle="{StaticResource DataGridCellCenter}"/>
                                <DataGridTextColumn Header="점유율" Binding="{Binding P_CustomRate}" Width="*"
                                  ElementStyle="{StaticResource DataGridCellRight}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="10*"/>
                                <ColumnDefinition Width="90*"/>
                            </Grid.ColumnDefinitions>
                            <DataGridRowHeader Grid.Column="0" Content="합 계" Style="{StaticResource RowHeaderStyle}"/>
                            <DataGrid x:Name="dgdPeriodTotal" Grid.Column="1" Style="{DynamicResource DataGridSet1}" HeadersVisibility="Column">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="출고 건수" Binding="{Binding P_TotalOutRoll}" Width="*" ElementStyle="{DynamicResource DataGridCellCenter}"/>
                                    <DataGridTextColumn Header="출고 수량" Binding="{Binding P_TotalOutQty}"  Width="*" ElementStyle="{DynamicResource DataGridCellCenter}"/>
                                    <DataGridTextColumn Header="입고 건수" Binding="{Binding P_TotalStuffRoll}" Width="*" ElementStyle="{DynamicResource DataGridCellCenter}" />
                                    <DataGridTextColumn Header="입고 수량" Binding="{Binding P_TotalStuffQty}"  Width="*" ElementStyle="{DynamicResource DataGridCellCenter}" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Grid>
                </TabItem>

                <TabItem x:Name="tiDay" Header="일일집계" Width="{Binding Path=ActualWidth, ElementName=c1}" Height="25">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="79*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <!--<RowDefinition Height="auto" />-->
                            <RowDefinition />
                            <RowDefinition Height="0.13*"/>
                        </Grid.RowDefinitions>

                        <DataGrid x:Name="grdMergeDays" IsReadOnly="True" Grid.ColumnSpan="2" 
                                  Style="{DynamicResource DataGridSet1}"
                                  SizeChanged="DataGrid_SizeChange" >

                            <DataGrid.Resources>
                                <Style TargetType="DataGridRow">
                                    <Style.Resources>
                                        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#3399FF"/>
                                        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="White"/>
                                        <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#3399FF" />
                                        <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="White"/>
                                    </Style.Resources>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding D_Color1}" Value="true">
                                            <Setter Property="Background" Value="#D8D8D8"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding D_Color2}" Value="true">
                                            <Setter Property="Background" Value="#BDBDBD"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.Resources>

                            <DataGrid.Columns>
                                <DataGridTextColumn Header="순서" Binding="{Binding D_NUM}" ElementStyle="{StaticResource DataGridCellCenter}"/>
                                <DataGridTextColumn Header="일자" Binding="{Binding D_IODate}" ElementStyle="{StaticResource DataGridCellCenter}"/>
                                <DataGridTextColumn Header="구분" Binding="{Binding D_Gbn}" ElementStyle="{StaticResource DataGridCellCenter}" />
                                <DataGridTextColumn Header="거래처" Binding="{Binding D_CustomName}" Width="*" ElementStyle="{StaticResource DataGridCellCenter}"/>
                                <DataGridTextColumn Header="품번" Binding="{Binding D_BuyerArticleNo}" Width="*" ElementStyle="{StaticResource DataGridCellLeft}"/>
                                <DataGridTextColumn Header="품명" Binding="{Binding D_Article}" Width="1.8*"/>
                                <DataGridTextColumn Header="건수" Binding="{Binding D_Roll}" Width="*"
                                         ElementStyle="{StaticResource DataGridCellRight}"/>
                                <DataGridTextColumn Header="수량" Binding="{Binding D_Qty}" Width="*"
                                         ElementStyle="{StaticResource DataGridCellRight}"/>
                                <DataGridTextColumn Header="단위" Binding="{Binding D_UnitClssName}" Width="*"
                                         ElementStyle="{StaticResource DataGridCellCenter}"/>
                                <DataGridTextColumn Header="금액" Binding="{Binding D_Amount}" Width="*"
                                         ElementStyle="{StaticResource DataGridCellRight}"/>
                                <DataGridTextColumn Header="부가세" Binding="{Binding D_VatAmount}" Width="*"
                                         ElementStyle="{StaticResource DataGridCellRight}"/>
                                <DataGridTextColumn Header="합계" Binding="{Binding D_TotAmount}" Width="*"
                                         ElementStyle="{StaticResource DataGridCellRight}"/>
                                <DataGridTextColumn Header="점유율" Binding="{Binding D_CustomRate}" Width="*"
                                         ElementStyle="{StaticResource DataGridCellRight}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        <Grid Grid.Row="1">
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="10*"/>
                                <ColumnDefinition Width="90*"/>
                            </Grid.ColumnDefinitions>
                            <DataGridRowHeader Grid.Column="0" Grid.Row="0" Content="구분" Style="{StaticResource RowHeaderStyle}"/>
                            <DataGridRowHeader x:Name="drhTotalOut_D" Grid.Column="0" Grid.Row="1" Content="출고" Style="{StaticResource RowHeaderStyle}"/>
                            <DataGridRowHeader Grid.Column="0" Grid.Row="2" Content="입고" Style="{StaticResource RowHeaderStyle}"/>
                            <Grid Grid.Row="0" Grid.Column="1">
                                <Grid.Resources>
                                    <Style TargetType="DataGridColumnHeader">
                                        <Setter Property="FontSize" Value="16"/>
                                        <Setter Property="Background" Value="#365fb1" />
                                        <Setter Property="BorderBrush" Value="White" />
                                        <Setter Property="Foreground" Value="White" />
                                        <Setter Property="BorderThickness" Value="0.7" />
                                        <Setter Property="FontWeight" Value="Thin" />
                                        <Setter Property="HorizontalContentAlignment" Value="Center" />
                                    </Style>
                                </Grid.Resources>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="{Binding ElementName=D_TotalOutRoll,Path=ActualWidth}"/>
                                    <ColumnDefinition Width="{Binding ElementName=D_TotalOutQty,Path=ActualWidth}"/>
                                    <ColumnDefinition Width="{Binding ElementName=D_TotalOutAmount,Path=ActualWidth}"/>
                                    <ColumnDefinition Width="{Binding ElementName=D_TotalOutVatAmount,Path=ActualWidth}"/>
                                    <ColumnDefinition Width="{Binding ElementName=D_TotalOutPrice,Path=ActualWidth}"/>
                                </Grid.ColumnDefinitions>
                                <DataGridColumnHeader Grid.Row="0" Grid.Column="0" Content="건수" FontSize="12"/>
                                <DataGridColumnHeader Grid.Row="0" Grid.Column="1" Content="수량" FontSize="12"/>
                                <DataGridColumnHeader Grid.Row="0" Grid.Column="2" Content="금액" FontSize="12"/>
                                <DataGridColumnHeader Grid.Row="0" Grid.Column="3" Content="부가세" FontSize="12"/>
                                <DataGridColumnHeader Grid.Row="0" Grid.Column="4" Content="합계" FontSize="12"/>
                            </Grid>
                            <DataGrid x:Name="dgdDaysOutTotal" Grid.Row="1" Grid.Column="1" HeadersVisibility="None"  BorderThickness="0" Padding="0" BorderBrush="{x:Null}" Margin="0">
                                <DataGrid.RowStyle>
                                    <Style TargetType="DataGridRow">
                                        <Setter Property="Height" Value="{Binding ElementName=drhTotalOut_D, Path=ActualHeight}"/>
                                    </Style>
                                </DataGrid.RowStyle>
                                <DataGrid.Columns>
                                    <DataGridTextColumn x:Name="D_TotalOutRoll" MinWidth="80" Width="*" Binding="{Binding D_TotalOutRoll}" ElementStyle="{DynamicResource DataGridCellCenter}"/>
                                    <DataGridTextColumn x:Name="D_TotalOutQty" MinWidth="80" Width="*"  Binding="{Binding D_TotalOutRoll}" ElementStyle="{DynamicResource DataGridCellCenter}"/>
                                    <DataGridTextColumn x:Name="D_TotalOutAmount" MinWidth="80"  Width="*"  Binding="{Binding D_TotalOutRoll}" ElementStyle="{DynamicResource DataGridCellCenter}"/>
                                    <DataGridTextColumn x:Name="D_TotalOutVatAmount" MinWidth="80"  Width="*"  Binding="{Binding D_TotalOutRoll}" ElementStyle="{DynamicResource DataGridCellCenter}"/>
                                    <DataGridTextColumn x:Name="D_TotalOutPrice" MinWidth="80"  Width="*"  Binding="{Binding D_TotalOutRoll}" ElementStyle="{DynamicResource DataGridCellCenter}"/>
                                </DataGrid.Columns>
                            </DataGrid>
                            <DataGrid x:Name="dgdDaysStuffTotal" Grid.Row="2" Grid.Column="1" HeadersVisibility="None"  BorderThickness="0" Padding="0"  BorderBrush="{x:Null}" Margin="0">
                                <DataGrid.Columns>
                                    <DataGridTextColumn x:Name="D_TotalStuffRoll" MinWidth="80" Width="*" Binding="{Binding D_TotalStuffRoll}" ElementStyle="{DynamicResource DataGridCellCenter}"/>
                                    <DataGridTextColumn x:Name="D_TotalStuffQty" MinWidth="80" Width="*"  Binding="{Binding D_TotalStuffQty}" ElementStyle="{DynamicResource DataGridCellCenter}"/>
                                    <DataGridTextColumn x:Name="D_TotalStuffAmount" MinWidth="80"  Width="*"  Binding="{Binding D_TotalStuffAmount}" ElementStyle="{DynamicResource DataGridCellCenter}"/>
                                    <DataGridTextColumn x:Name="D_TotalStuffVatAmount" MinWidth="80"  Width="*"  Binding="{Binding D_TotalStuffVatAmount}" ElementStyle="{DynamicResource DataGridCellCenter}"/>
                                    <DataGridTextColumn x:Name="D_TotalStuffPrice" MinWidth="80"  Width="*"  Binding="{Binding D_TotalStuffPrice}" ElementStyle="{DynamicResource DataGridCellCenter}"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Grid>
                </TabItem>

                <TabItem x:Name="tiMonth_V" Header="월별집계(세로)" Width="{Binding Path=ActualWidth, ElementName=c1}" Height="25" >
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition Height="0.13*"/>
                        </Grid.RowDefinitions>
                        <DataGrid x:Name="grdMergeMonth_V" IsReadOnly="True" SizeChanged="DataGrid_SizeChange"
                                  Style="{DynamicResource DataGridSet1}">

                            <DataGrid.Resources>
                                <Style TargetType="DataGridRow">
                                    <Style.Resources>
                                        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#3399FF"/>
                                        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="White"/>
                                        <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#3399FF" />
                                        <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="White"/>
                                    </Style.Resources>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding V_Color1}" Value="true">
                                            <Setter Property="Background" Value="#D8D8D8"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding V_Color2}" Value="true">
                                            <Setter Property="Background" Value="#BDBDBD"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.Resources>

                            <DataGrid.Columns>
                                <DataGridTextColumn Header="순서" Binding="{Binding V_NUM}" ElementStyle="{StaticResource DataGridCellCenter}"/>
                                <DataGridTextColumn Header="년월" Binding="{Binding V_IODate}" ElementStyle="{StaticResource DataGridCellCenter}"/>
                                <DataGridTextColumn Header="구분" Binding="{Binding V_Gbn}" ElementStyle="{StaticResource DataGridCellCenter}"/>
                                <DataGridTextColumn Header="거래처" Binding="{Binding V_CustomName}" Width="*" ElementStyle="{StaticResource DataGridCellCenter}"/>
                                <DataGridTextColumn Header="품번" Binding="{Binding V_BuyerArticleNo}" Width="*"  ElementStyle="{StaticResource DataGridCellLeft}"/>
                                <DataGridTextColumn Header="품명" Binding="{Binding V_Article}" Width="*" ElementStyle="{StaticResource DataGridCellLeft}"/>
                                <DataGridTextColumn Header="건수" Binding="{Binding V_Roll}" Width="*" ElementStyle="{StaticResource DataGridCellRight}"/>
                                <DataGridTextColumn Header="수량" Binding="{Binding V_Qty}" Width="*" ElementStyle="{StaticResource DataGridCellRight}"/>
                                <DataGridTextColumn Header="단위" Binding="{Binding V_UnitClssName}" Width="*" ElementStyle="{StaticResource DataGridCellCenter}"/>
                                <DataGridTextColumn Header="점유율" Binding="{Binding V_CustomRate}" Width="*" ElementStyle="{StaticResource DataGridCellRight}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="10*"/>
                                <ColumnDefinition Width="90*"/>
                            </Grid.ColumnDefinitions>
                            <DataGridRowHeader Grid.Column="0" Content="합 계" Style="{StaticResource RowHeaderStyle}"/>
                            <DataGrid x:Name="dgdMonthVtotal" Grid.Column="1" Style="{DynamicResource DataGridSet1}" HeadersVisibility="Column">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="출고 건수" Binding="{Binding V_TotalOutRoll}" Width="*" ElementStyle="{DynamicResource DataGridCellCenter}"/>
                                    <DataGridTextColumn Header="출고 수량" Binding="{Binding V_TotalOutQty}"  Width="*" ElementStyle="{DynamicResource DataGridCellCenter}"/>
                                    <DataGridTextColumn Header="입고 건수" Binding="{Binding V_TotalStuffRoll}" Width="*" ElementStyle="{DynamicResource DataGridCellCenter}" Visibility="Hidden"/>
                                    <DataGridTextColumn Header="입고 수량" Binding="{Binding V_TotalStuffQty}"  Width="*" ElementStyle="{DynamicResource DataGridCellCenter}" Visibility="Hidden"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Grid>
                </TabItem>

                <TabItem x:Name="tiMonth_H" Header="월별집계(가로)" Width="{Binding Path=ActualWidth, ElementName=c1}" Height="25">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition Height="0.21*"/>
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="10*"/>
                                <RowDefinition Height="90*"/>
                            </Grid.RowDefinitions>
                            <ScrollViewer x:Name="dgdMergeMonth_H_SV" Grid.Column="0" HorizontalScrollBarVisibility="Hidden" 
                          ScrollViewer.CanContentScroll="False" VerticalScrollBarVisibility="Hidden">
                                <ScrollViewer.Resources>
                                    <Style TargetType="DataGridColumnHeader">
                                        <Setter Property="FontSize" Value="16"/>
                                        <Setter Property="Background" Value="#365fb1" />
                                        <Setter Property="BorderBrush" Value="White" />
                                        <Setter Property="Foreground" Value="White" />
                                        <Setter Property="BorderThickness" Value="0.7" />
                                        <Setter Property="FontWeight" Value="Thin" />
                                        <Setter Property="HorizontalContentAlignment" Value="Center" />
                                    </Style>
                                </ScrollViewer.Resources>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="{Binding ElementName=H_NUM, Path=ActualWidth}"/>
                                        <ColumnDefinition Width="{Binding ElementName=H_Gbn, Path=ActualWidth}"/>
                                        <ColumnDefinition Width="{Binding ElementName=H_CustomName, Path=ActualWidth}"/>
                                        <ColumnDefinition Width="{Binding ElementName=H_Article, Path=ActualWidth}"/>
                                        <ColumnDefinition Width="{Binding ElementName=H_UnitClssName, Path=ActualWidth}"/>

                                        <ColumnDefinition Width="{Binding ElementName=H_TotalMonthRoll, Path=ActualWidth}"/>
                                        <ColumnDefinition Width="{Binding ElementName=H_TotalMonthQty, Path=ActualWidth}"/>
                                        <ColumnDefinition Width="{Binding ElementName=H_BaseMonthRoll, Path=ActualWidth}"/>
                                        <ColumnDefinition Width="{Binding ElementName=H_BaseMonthQty, Path=ActualWidth}"/>
                                        <ColumnDefinition Width="{Binding ElementName=H_Add1MonthRoll, Path=ActualWidth}"/>
                                        <ColumnDefinition Width="{Binding ElementName=H_Add1MonthQty, Path=ActualWidth}"/>
                                        <ColumnDefinition Width="{Binding ElementName=H_Add2MonthRoll, Path=ActualWidth}"/>
                                        <ColumnDefinition Width="{Binding ElementName=H_Add2MonthQty, Path=ActualWidth}"/>
                                    </Grid.ColumnDefinitions>
                                    <DataGridColumnHeader Grid.Row="0" Grid.RowSpan="2" Grid.Column="0" Content="순" FontSize="12"/>
                                    <DataGridColumnHeader Grid.Row="0" Grid.RowSpan="2" Grid.Column="1" Content="구분" FontSize="12"/>
                                    <DataGridColumnHeader Grid.Row="0" Grid.RowSpan="2" Grid.Column="2" Content="거래처" FontSize="12"/>
                                    <DataGridColumnHeader Grid.Row="0" Grid.RowSpan="2" Grid.Column="3" Content="품명" FontSize="12"/>
                                    <DataGridColumnHeader Grid.Row="0" Grid.RowSpan="2" Grid.Column="4" Content="단위" FontSize="12"/>

                                    <!--합계-->
                                    <DataGridColumnHeader Grid.Row="0" Grid.ColumnSpan="2" Grid.Column="5"  Content="합계" FontSize="12"/>
                                    <DataGridColumnHeader Grid.Row="1" Grid.Column="5" Content="수량" FontSize="12"/>
                                    <DataGridColumnHeader Grid.Row="1" Grid.Column="6" Content="금액" FontSize="12"/>

                                    <!--기준월-->
                                    <DataGridColumnHeader Grid.Row="0" Grid.ColumnSpan="2" Grid.Column="7"  Content="기준월" FontSize="12"/>
                                    <DataGridColumnHeader Grid.Row="1" Grid.Column="7" Content="수량" FontSize="12"/>
                                    <DataGridColumnHeader Grid.Row="1" Grid.Column="8" Content="금액" FontSize="12"/>

                                    <!--기준+2월-->
                                    <DataGridColumnHeader Grid.Row="0" Grid.ColumnSpan="2" Grid.Column="9"  Content="기준+1월" FontSize="12"/>
                                    <DataGridColumnHeader Grid.Row="1" Grid.Column="9" Content="수량" FontSize="12"/>
                                    <DataGridColumnHeader Grid.Row="1" Grid.Column="10" Content="금액" FontSize="12"/>

                                    <!--기준+2월-->
                                    <DataGridColumnHeader Grid.Row="0" Grid.ColumnSpan="2" Grid.Column="11"  Content="기준+2월" FontSize="12"/>
                                    <DataGridColumnHeader Grid.Row="1" Grid.Column="11" Content="수량" FontSize="12"/>
                                    <DataGridColumnHeader Grid.Row="1" Grid.Column="12" Content="금액" FontSize="12"/>
                                </Grid>
                            </ScrollViewer>
                            <DataGrid x:Name="grdMergeMonth_H" IsReadOnly="True" Style="{DynamicResource DataGridMultiLine}" Grid.Row="1"
                       HeadersVisibility="None">
                                <DataGrid.Resources>
                                    <Style TargetType="DataGridRow">
                                        <Style.Resources>
                                            <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#3399FF"/>
                                            <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="White"/>
                                            <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#3399FF" />
                                            <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="White"/>
                                        </Style.Resources>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding H_Color1}" Value="true">
                                                <Setter Property="Background" Value="#D8D8D8"/>
                                            </DataTrigger>

                                            <DataTrigger Binding="{Binding H_Color2}" Value="true">
                                                <Setter Property="Background" Value="#BDBDBD"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGrid.Resources>

                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="순" x:Name="H_NUM" Binding="{Binding H_NUM}" MinWidth="50" IsReadOnly="True" CellStyle="{DynamicResource DataGridCellAndCentering}"/>
                                    <DataGridTextColumn Header="구분" x:Name="H_Gbn" Binding="{Binding H_Gbn}" MinWidth="50" IsReadOnly="True" CellStyle="{DynamicResource DataGridCellAndCentering}"/>
                                    <DataGridTextColumn Header="거래처" x:Name="H_CustomName" Binding="{Binding H_CustomName}" MinWidth="80" IsReadOnly="True" CellStyle="{DynamicResource DataGridCellAndCentering}"/>
                                    <DataGridTextColumn Header="품명" x:Name="H_Article" Binding="{Binding H_Article}" MinWidth="80" IsReadOnly="True" CellStyle="{DynamicResource DataGridCellAndCentering}"/>
                                    <DataGridTextColumn Header="단위" x:Name="H_UnitClssName" Binding="{Binding H_UnitClssName}" MinWidth="80" IsReadOnly="True" CellStyle="{DynamicResource DataGridCellAndCentering}"/>

                                    <!--합계-->
                                    <DataGridTextColumn Header="수량" x:Name="H_TotalMonthRoll" Binding="{Binding H_TotalMonthQty}" MinWidth="80" Width="*" IsReadOnly="True" CellStyle="{DynamicResource NumericDataGridCell}"/>
                                    <DataGridTextColumn Header="금액" x:Name="H_TotalMonthQty" Binding="{Binding H_TotalMonthAmount}" MinWidth="80" Width="*" IsReadOnly="True" CellStyle="{DynamicResource NumericDataGridCell}"/>

                                    <!--기준월-->
                                    <DataGridTextColumn Header="수량" x:Name="H_BaseMonthRoll" Binding="{Binding H_BaseMonthQty}" MinWidth="80" Width="*" IsReadOnly="True" CellStyle="{DynamicResource NumericDataGridCell}"/>
                                    <DataGridTextColumn Header="금액" x:Name="H_BaseMonthQty" Binding="{Binding H_BaseMonthAmount}" MinWidth="80" Width="*" IsReadOnly="True" CellStyle="{DynamicResource NumericDataGridCell}"/>

                                    <!--기준+1월-->
                                    <DataGridTextColumn Header="수량" x:Name="H_Add1MonthRoll" Binding="{Binding H_Add1MonthQty}" MinWidth="80" Width="*" IsReadOnly="True" CellStyle="{DynamicResource NumericDataGridCell}"/>
                                    <DataGridTextColumn Header="금액" x:Name="H_Add1MonthQty" Binding="{Binding H_Add1MonthAmount}" MinWidth="80" Width="*" IsReadOnly="True" CellStyle="{DynamicResource NumericDataGridCell}"/>

                                    <!--기준+2월-->
                                    <DataGridTextColumn Header="수량" x:Name="H_Add2MonthRoll" Binding="{Binding H_Add2MonthQty}" MinWidth="80" Width="*"  IsReadOnly="True" CellStyle="{DynamicResource NumericDataGridCell}"/>
                                    <DataGridTextColumn Header="금액" x:Name="H_Add2MonthQty" Binding="{Binding H_Add2MonthAmount}" MinWidth="80" Width="*" IsReadOnly="True" CellStyle="{DynamicResource NumericDataGridCell}"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                        <Grid Grid.Row="1">
                            <Grid Grid.Row="1">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="40*"/>
                                    <RowDefinition Height="15.8*"/>
                                    <RowDefinition Height="15.8*"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="10*"/>
                                    <ColumnDefinition Width="90*"/>
                                </Grid.ColumnDefinitions>
                                <DataGridRowHeader Grid.Column="0" Grid.Row="0" Content="구분" Style="{StaticResource RowHeaderStyle}"/>
                                <DataGridRowHeader x:Name="drhTotalOut_H" Grid.Column="0" Grid.Row="1" Content="출고" Style="{StaticResource RowHeaderStyle}"/>
                                <DataGridRowHeader Grid.Column="0" Grid.Row="2" Content="입고" Style="{StaticResource RowHeaderStyle}"/>
                                <Grid Grid.Column="1" Grid.Row="0">
                                    <ScrollViewer x:Name="dgddgdMonth_H_Total_SV" Grid.Column="0" HorizontalScrollBarVisibility="Hidden" 
                                                 ScrollViewer.CanContentScroll="False" VerticalScrollBarVisibility="Hidden">
                                        <ScrollViewer.Resources>
                                            <Style TargetType="DataGridColumnHeader">
                                                <Setter Property="FontSize" Value="16"/>
                                                <Setter Property="Background" Value="#365fb1" />
                                                <Setter Property="BorderBrush" Value="White" />
                                                <Setter Property="Foreground" Value="White" />
                                                <Setter Property="BorderThickness" Value="0.7" />
                                                <Setter Property="FontWeight" Value="Thin" />
                                                <Setter Property="HorizontalContentAlignment" Value="Center" />
                                            </Style>
                                        </ScrollViewer.Resources>
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="*"/>
                                                <RowDefinition Height="*"/>
                                            </Grid.RowDefinitions>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="{Binding ElementName=H_TotalOutRoll, Path=ActualWidth}"/>
                                                <ColumnDefinition Width="{Binding ElementName=H_TotalOutQty, Path=ActualWidth}"/>
                                                <ColumnDefinition Width="{Binding ElementName=H_TotalBaseOutRoll, Path=ActualWidth}"/>
                                                <ColumnDefinition Width="{Binding ElementName=H_TotalBaseOutQty, Path=ActualWidth}"/>
                                                <ColumnDefinition Width="{Binding ElementName=H_TotalAdd1OutRoll, Path=ActualWidth}"/>
                                                <ColumnDefinition Width="{Binding ElementName=H_TotalAdd1OutQty, Path=ActualWidth}"/>
                                                <ColumnDefinition Width="{Binding ElementName=H_TotalAdd2OutRoll, Path=ActualWidth}"/>
                                                <ColumnDefinition Width="{Binding ElementName=H_TotalAdd2OutQty, Path=ActualWidth}"/>
                                            </Grid.ColumnDefinitions>
                                            <DataGridColumnHeader Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Content="합계" FontSize="12"/>
                                            <DataGridColumnHeader Grid.Row="1" Grid.Column="0" Content="건수" FontSize="12"/>
                                            <DataGridColumnHeader Grid.Row="1" Grid.Column="1" Content="수량" FontSize="12"/>

                                            <DataGridColumnHeader Grid.Row="0" Grid.Column="2" Grid.ColumnSpan="2" Content="기준월" FontSize="12"/>
                                            <DataGridColumnHeader Grid.Row="1" Grid.Column="2" Content="건수" FontSize="12"/>
                                            <DataGridColumnHeader Grid.Row="1" Grid.Column="3" Content="수량" FontSize="12"/>

                                            <DataGridColumnHeader Grid.Row="0" Grid.Column="4" Grid.ColumnSpan="2" Content="기준+1월" FontSize="12"/>
                                            <DataGridColumnHeader Grid.Row="1" Grid.Column="4" Content="건수" FontSize="12"/>
                                            <DataGridColumnHeader Grid.Row="1" Grid.Column="5" Content="수량" FontSize="12"/>

                                            <DataGridColumnHeader Grid.Row="0" Grid.Column="6" Grid.ColumnSpan="2" Content="기준+2월" FontSize="12"/>
                                            <DataGridColumnHeader Grid.Row="1" Grid.Column="6" Content="건수" FontSize="12"/>
                                            <DataGridColumnHeader Grid.Row="1" Grid.Column="7" Content="수량" FontSize="12"/>
                                        </Grid>
                                    </ScrollViewer>
                                </Grid>
                                <DataGrid x:Name="dgdMonthHOutTotal" Grid.Row="1" Grid.Column="1" HeadersVisibility="None"  BorderThickness="0" Padding="0" BorderBrush="{x:Null}" Margin="0">
                                    <DataGrid.RowStyle>
                                        <Style TargetType="DataGridRow">
                                            <Setter Property="Height" Value="{Binding ElementName=drhTotalOut_H, Path=ActualHeight}"/>
                                        </Style>
                                    </DataGrid.RowStyle>
                                    <DataGrid.Columns>
                                        <DataGridTextColumn x:Name="H_TotalOutRoll" MinWidth="80" Width="*" Binding="{Binding H_TotalOutRoll}" ElementStyle="{DynamicResource DataGridCellCenter}"/>
                                        <DataGridTextColumn x:Name="H_TotalOutQty" MinWidth="80" Width="*"  Binding="{Binding H_TotalOutQty}" ElementStyle="{DynamicResource DataGridCellCenter}"/>
                                        <DataGridTextColumn x:Name="H_TotalBaseOutRoll" MinWidth="80" Width="*" Binding="{Binding H_TotalBaseOutRoll}" ElementStyle="{DynamicResource DataGridCellCenter}"/>
                                        <DataGridTextColumn x:Name="H_TotalBaseOutQty" MinWidth="80" Width="*"  Binding="{Binding H_TotalBaseOutQty}" ElementStyle="{DynamicResource DataGridCellCenter}"/>
                                        <DataGridTextColumn x:Name="H_TotalAdd1OutRoll" MinWidth="80" Width="*"  Binding="{Binding H_TotalAdd1OutRoll}" ElementStyle="{DynamicResource DataGridCellCenter}"/>
                                        <DataGridTextColumn x:Name="H_TotalAdd1OutQty" MinWidth="80" Width="*"  Binding="{Binding H_TotalAdd1OutQty}" ElementStyle="{DynamicResource DataGridCellCenter}"/>
                                        <DataGridTextColumn x:Name="H_TotalAdd2OutRoll" MinWidth="80" Width="*"  Binding="{Binding H_TotalAdd2OutRoll}" ElementStyle="{DynamicResource DataGridCellCenter}"/>
                                        <DataGridTextColumn x:Name="H_TotalAdd2OutQty" MinWidth="80" Width="*"  Binding="{Binding H_TotalAdd2OutQty}" ElementStyle="{DynamicResource DataGridCellCenter}"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                                <DataGrid x:Name="dgdMonthHStuffTotal" Grid.Row="2" Grid.Column="1" HeadersVisibility="None"  BorderThickness="0" Padding="0"  BorderBrush="{x:Null}" Margin="0">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn x:Name="H_TotalStuffRoll" MinWidth="80" Width="*" Binding="{Binding H_TotalStuffRoll}" ElementStyle="{DynamicResource DataGridCellCenter}"/>
                                        <DataGridTextColumn x:Name="H_TotalStuffQty" MinWidth="80" Width="*"  Binding="{Binding H_TotalStuffQty}" ElementStyle="{DynamicResource DataGridCellCenter}"/>
                                        <DataGridTextColumn x:Name="H_TotalBaseStuffRoll" MinWidth="80" Width="*" Binding="{Binding H_TotalBaseStuffRoll}" ElementStyle="{DynamicResource DataGridCellCenter}"/>
                                        <DataGridTextColumn x:Name="H_TotalBaseStuffQty" MinWidth="80" Width="*"  Binding="{Binding H_TotalBaseStuffQty}" ElementStyle="{DynamicResource DataGridCellCenter}"/>
                                        <DataGridTextColumn x:Name="H_TotalAdd1StuffRoll" MinWidth="80" Width="*" Binding="{Binding H_TotalAdd1StuffRoll}" ElementStyle="{DynamicResource DataGridCellCenter}"/>
                                        <DataGridTextColumn x:Name="H_TotalAdd1StuffQty" MinWidth="80" Width="*"  Binding="{Binding H_TotalAdd1StuffQty}" ElementStyle="{DynamicResource DataGridCellCenter}"/>
                                        <DataGridTextColumn x:Name="H_TotalAdd2StuffRoll" MinWidth="80" Width="*" Binding="{Binding H_TotalAdd2StuffRoll}" ElementStyle="{DynamicResource DataGridCellCenter}"/>
                                        <DataGridTextColumn x:Name="H_TotalAdd2StuffQty" MinWidth="80" Width="*"  Binding="{Binding H_TotalAdd2StuffQty}" ElementStyle="{DynamicResource DataGridCellCenter}"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Grid>
                        </Grid>
                    </Grid>

                    <!--<Grid>
          <Grid.RowDefinitions>
              -->
                    <!--<RowDefinition Height="auto" />-->
                    <!--
              <RowDefinition />
              -->
                    <!--<RowDefinition Height="0.1*"/>-->
                    <!--
          </Grid.RowDefinitions>

          <DataGrid x:Name="grdMergeMonth_H" IsReadOnly="True" Grid.ColumnSpan="2" Style="{DynamicResource DataGridSet1}"
                    SizeChanged="DataGrid_SizeChange">

              <DataGrid.Resources>
                  <Style TargetType="DataGridRow">
                      <Style.Resources>
                          <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#3399FF"/>
                          <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="White"/>
                          <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#3399FF" />
                          <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="White"/>
                      </Style.Resources>
                      <Style.Triggers>
                          <DataTrigger Binding="{Binding H_Color1}" Value="true">
                              <Setter Property="Background" Value="#D8D8D8"/>
                          </DataTrigger>

                          <DataTrigger Binding="{Binding H_Color2}" Value="true">
                              <Setter Property="Background" Value="#BDBDBD"/>
                          </DataTrigger>
                      </Style.Triggers>
                  </Style>
              </DataGrid.Resources>

              <DataGrid.Columns>
                  <DataGridTextColumn Header="순서" Binding="{Binding H_NUM}" />
                  <DataGridTextColumn Header="구분" Binding="{Binding H_Gbn}" ElementStyle="{StaticResource DataGridCellCenter}"/>
                  <DataGridTextColumn Header="거래처" Binding="{Binding H_CustomName}" Width="*" ElementStyle="{StaticResource DataGridCellCenter}"/>
                  <DataGridTextColumn Header="품명" Binding="{Binding H_Article}" Width="2.3*"/>
                  <DataGridTextColumn Header="단위" Binding="{Binding H_UnitClssName}" Width="*"
                                      ElementStyle="{StaticResource DataGridCellRight}"/>
                  <DataGridTextColumn Header="합계건수" Binding="{Binding H_roll13}" Width="*"
                                      ElementStyle="{StaticResource DataGridCellRight}"/>
                  <DataGridTextColumn Header="합계수량" Binding="{Binding H_Qty13}" Width="*"
                                      ElementStyle="{StaticResource DataGridCellRight}"/>
                  <DataGridTextColumn Header="한달전건수" Binding="{Binding H_roll10}" Width="*"
                                      ElementStyle="{StaticResource DataGridCellRight}"/>
                  <DataGridTextColumn Header="한달전수량" Binding="{Binding H_Qty10}" Width="*"
                                      ElementStyle="{StaticResource DataGridCellRight}"/>
                  <DataGridTextColumn Header="두달전건수" Binding="{Binding H_roll11}" Width="*"
                                      ElementStyle="{StaticResource DataGridCellRight}"/>
                  <DataGridTextColumn Header="두달전수량" Binding="{Binding H_Qty11}" Width="*"
                                      ElementStyle="{StaticResource DataGridCellRight}"/>
                  <DataGridTextColumn Header="세달전건수" Binding="{Binding H_roll12}" Width="*"
                                      ElementStyle="{StaticResource DataGridCellRight}"/>
                  <DataGridTextColumn Header="세달전수량" Binding="{Binding H_Qty12}" Width="*"
                                      ElementStyle="{StaticResource DataGridCellRight}"/>
              </DataGrid.Columns>
          </DataGrid>

      </Grid>-->
                </TabItem>
            </TabControl>
        </Grid>

        <!-- 화면 하단, 구매거래처 -->
        <Grid Margin="2" Grid.Column="0" Grid.Row="2">
            <StackPanel>
                <TextBlock Text="☞ 품목은 거래처별 품목으로 등록된 품목 List입니다."></TextBlock>
                <TextBlock x:Name="txtblMessage" Text="☞ 최근 3개월로 집계기간이 고정됩니다."></TextBlock>
            </StackPanel>

        </Grid>

    </Grid>
</UserControl>
